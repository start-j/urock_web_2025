// document.querySelectorAll('.tab-menu').forEach((tab, index) => {
//   tab.addEventListener('click', () => {
//     const focusBar = document.querySelector('.focus-bar');
//     const offset = index * 48; // gap 크기
//     focusBar.style.left = `calc(50% + ${offset}px)`;
//   });
// });